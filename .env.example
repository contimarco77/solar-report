# Copy this file to .env and set the key. Never commit .env.
ANTHROPIC_API_KEY=

# Only needed if source.kind in config.yaml is "home_assistant" (not yet
# supported in v0.1, see CLAUDE.md roadmap for v0.2).
# HA_TOKEN=
